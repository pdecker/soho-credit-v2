services:
  - type: web
    name: agentcredit-api
    runtime: node
    buildCommand: npm install && npm run build && npm run migrate
    startCommand: node dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
